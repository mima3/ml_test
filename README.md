
## VSCodeを使う場合
Shift+cmd+p or 表示→コマンドパレット
Dev Containers: Rebuild and Reopen in Container

or

Dev Containers: Reopen in Container


## 使わないケース

```
# ビルド
docker compose build

# 起動
docker compose up -d

# 中に入る
docker compose exec ml-cpu bash
```
